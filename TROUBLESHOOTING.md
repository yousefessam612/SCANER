# حل المشكلات / Troubleshooting

## مشكلة: التطبيق يعالج جزءًا فقط من الكتاب (يتوقف عند صفحة معينة)

**الأعراض**: الكتاب 500 صفحة لكن العملية تتوقف عند الصفحة 330 (أو 15 صفحة في ملف صغير، حسب مكان المشكلة)، والتقرير يقول إن صفحات لم تُعالج.

**السبب الجذري (مُشخَّص بالقياس)**: كان Tesseract يتجمّد أحيانًا على صفحة واحدة، فيحتل خيط OCR بالكامل حتى انتهاء المهلة (900 ثانية لصفحة واحدة في القياس!). سبب التجمّد معروف على ويندوز: كل عملية Tesseract مضبوطة على استخدام **كل أنوية المعالج** داخليًا (OpenMP)، وبوجود عدة عمليات OCR متوازية يحدث إفراط حاد في الحجز (oversubscription) ينتهي بتجمّد. الصفحات المتبقية لا تُحاول أصلًا، فيبدو الكتاب وكأن له «حد صفحات».

**العلاج في اقرأ (مُطبَّق ومُختبر)**:
1. تثبيت كل عملية Tesseract على **خيط واحد** (`OMP_THREAD_COUNT=1`)؛ التوازي يأتي من عدد العمال الذي نتحكم به — لا إفراط في الحجز ولا تجمّد.
2. **إنهاء شجرة العملية** فورًا عند انتهاء المهلة (taskkill/T) حتى لا تبقى مقابض ملفات معلّقة.
3. مهلة لكل صفحة (300 ثانية) وأرشفة النتائج الوسيطة لحظيًا.
4. **حارس توقف (watchdog)**: إذا ماتت خيوط العمل أو توقف التقدّم 45 دقيقة، تُنهي المهمة وتُسجّل الصفحات المتبقية صراحةً كفاشلة — فلا يبقى البرنامج معلّقًا ولا يُبلّغ عن نجاح جزئي كأنه كامل.
5. تقرير نهائي دائمًا: «500 مكتملة، 0 فاشلة» أو يذكر الصفحات الفاشلة التي لم تُحاول.

**النتيجة المقيسة**: كتاب 500 صفحة حقيقي: **500/500 صفحة، 0 فاشلة، 415 ثانية** (كان يتوقف عند ~330 قبل الإصلاح). وملف 100 صفحة: **100/100، دقيقة و42 ثانية**.

**للتحقق بنفسك**: شغّل `python tools\validate_outputs.py "مجلد الإخراج"` للتأكد أن المخرجات نص نظيف، وتابع السجل (`عرض ← فتح ملف السجل`) لرؤية أي تحذير.

## مشكلة حرجة (1): الناتج نص آلّي — أرقام وإحداثيات وقيم ثقة

**الأعراض**: جزء من النص يخرج سليمًا وجزء يخرج بالشكل التالي (بيانات آلية):

```
5	1	1	2	4	1	2936	2338	114	83	91.518349	وفي
5	1	1	2	4	2	2711	2334	184	76	91.665031	العصر
5	1	1	2	4	3	2474	2334	193	74	91.696533	الحديث
```

**السبب**: ملف PDF يحمل «طبقة نص» هي **مُخرَج OCR آلي خام** (أرقام الهندسة + قيم الثقة `91.518349` + الكلمات) دُفنت داخل الملف بواسطة أداة أخرى. أي برنامج يثق بهذه الطبقة سيُخرج هذه الأرقام.

**الحل في اقرأ**: كاشف «البيانات الآلية» يقيس ثلاثة مؤشرات (بلا AI):

| المؤشر | ملف آلّي | نص طبيعي | جدول أرقام مشروع |
|---|---|---|---|
| كلمات نصية ÷ الكل | **0.08** | 0.83–1.00 | 0.55 |
| أحرف جدولة (tab) | **142** | 0 | 0 |
| أرقام ثقة (4+ منازل عشرية) | **12** | 0 | 0 |

عند اكتشاف أي توليفة منها يتجاهل اقرأ الطبقة **ويُشغّل OCR على الصورة المرئية** → نص عربي حقيقي. والصفحات المعالجة تُدرج في تقرير الجودة.

**مهمّ**: جداول الأرقام المشروعة (إحصاءات، دراسات) والنصوص العلمية العشرية **لا تُعلَّم خطأً** — مُختبرة، شروط الكشف تجميعية ولا تتحقق فيها.

## مشكلة حرجة (2): النص العربي يخرج مشوّهًا / معكوسًا / «حروف وحركات متبعثرة»

**الأعراض**: المستند يظهر سليمًا في القارئ، لكن الناتج نصٌّ فاسد مثل:

```
ًٌُح ٍْٓٞس ػز ٝٓٔخ َٟٝ٣ ٕأ ٍَُٞٓح ِٛ٠ هللا ٚػ ِ٤ ِْٓٝ
```

أو كلمات معكوسة مثل `ةيبرعلا` بدل `العربية`. **كل البرامج الأخرى تُخرج نفس الشيء.**

**السبب**: ملف PDF نفسه يحمل «طبقة نص تالفة» — إما أن أداة أخرى أنتجت OCR رديئًا وحفظته داخل الملف، أو أن خريطة ترميز الخط (ToUnicode) معطوبة. الصفحة المرئية سليمة تمامًا، لكن النص القابل للاستخراج منها فاسد. أي برنامج يثق بهذه الطبقة سيُخرج الفساد نفسه — ولهذا تتشابه كل البرامج في الخطأ.

**الحل في اقرأ**: يفحص اقرأ الطبقة النصية العربية إحصائيًا (بلا AI) بكاشف مبني على قياسات حقيقية:

| المؤشر | النص السليم | طبقة تالفة |
|---|---|---|
| علامات التشكيل ÷ الحروف | حتى 1.0 | 1.5–2.0+ |
| أزواج علامات متتالية غير مشروعة | ~0 | أكثر من عدد الحروف |
| حروف من نطاقات فارسية/أردية (ػ ؼ) | صفر | موجودة |
| كلمات تبدأ بـ«ال» | 25–50% | ~صفر |

عند اكتشاف أي من هذه العلامات يتجاهل اقرأ الطبقة التالفة **ويشغّل OCR على الصورة المرئية** — فالنتيجة نص عربي صحيح. تُدرج الصفحات المعالجة بهذه الطريقة في تقرير الجودة تحت «صفحات استُخدم معها OCR لأن طبقة النص الأصلية تالفة».

**ملاحظات**:
- النص المشكول (كتب دينية) **لا يُعلَّم خطأً**: الأنماط غير المشروعة فقط هي ما يُكتشف (شدة+حركة مسموح، والتشكيل الطبيعي لا يبلغ العتبات أبدًا) — مُختبر على البسملة والأحاديث والآيات.
- للتعطيل: الإعدادات ← «الثقة بالطبقات النصية التالفة» — لكن لا ننصح بذلك.
- للكتب المطبوعة جيدًا، النموذج «best» المستخدم تلقائيًا للعربية يعطي نتائج أدق من الطبقة التالفة بكثير.

**تحسين الدقة بعد الكشف** (قياسات حقيقية على صفحة عربية صعبة 300dpi):

| الإعداد | الثقة | ملاحظة |
|---|---|---|
| متوازن + PSM 3 (افتراضي) | 89.3 | جيد |
| **متوازن + PSM 4 (عمود واحد)** | **90.0** | نص أكمل — الأفضل للكتب العربية ذات العمود الواحد |
| دقة عالية (400dpi) + PSM 3 | 89.4 | تقسيم أنظف للعناوين |
| متوازن + PSM 6 | 83.8 | **لا تستخدمه للعربية** — يقسّم الكلمات خطأً |

للكتب العربية العادية جرّب: الجودة «متوازن» + PSM «عمود واحد — أفضل للكتب العربية» من الإعدادات.

**أي لغة أختار؟** (قياسات حقيقية على صفحات عربية، نموذج best):

| الصفحة | `ara` وحده | `ara+eng` |
|---|---|---|
| عربية بها كلمات لاتينية عرضية | 88.8 — أسقط الكلمات الإنجليزية | **90.0 — التقطها كلها** |
| عربية + إنجليزية مختلطة | 71.4 — تدهور واضح | **92.8** |

**الخلاصة: «العربية + الإنجليزية» هي الأفضل دائمًا** للكتب العربية (وهي الافتراضية في البرنامج) — حتى للصفحات العربية الخالصة لا تخسر شيئًا، وتكسب صحة أي كلمة لاتينية. لا تختَر «العربية» وحدها إلا إذا أردت تقليل الالتباس بين الحروف المتشابهة بين اللغتين في مسح رديء جدًا.

## التثبيت

**المثبت يفشل بـ"not enough disk space"**
تحتاج ~1 GB حرًا (التطبيق 420 MB بعد التثبيت + مسافة مؤقتة للضغط).

**الرسالة "This app can't run on your PC"**
اقرأ لنظام 64-bit فقط (Windows 10/11). لا يوجد إصدار 32-bit.

**أيقونة سطح المكتب لم تُنشأ**
كان خيارها غير مفعّل افتراضيًا (Alt+D في المثبت لتفعيله)، أو أنشئها يدويًا من قائمة ابدأ ← زر سياق ← إرسال إلى ← سطح المكتب.

## التشغيل

**"tesseract executable not found" عند بدء التحويل**
1. القائمة "إعدادات" ← حقل "مسار Tesseract" ← اتركه فارغًا للكشف التلقائي، أو ضع `{مجلد التثبيت}\runtime\tesseract\tesseract.exe`.
2. زر "اختبار" ينفذ فحصًا ذاتيًا حقيقيًا (يرسم صفحة ويتعرف عليها) ويعرض النتيجة.
3. من سطر الأوامر: `IqraCLI.exe selftest`.

**النتيجة العربية رديئة/مبعثرة**
- استخدم ملف "دقة عالية" أو "مسح ضعيف" بدل "سريع".
- تأكد أن لغة التعرف "العربية + الإنجليزية" للمستندات المختلطة.
- لو المصدر PDF نصي لكن نصه معكوس (PDF سيئ الصنع): الإعدادات ← "إصلاح النص العربي المعكوس" (مفعّل افتراضيًا).
- الأرقام مشوشة؟ جرّب سياسة الأرقام "تحويل إلى ١٢٣" أو "تحويل إلى 123".

**نتيجة فارغة لصفحات (0 كلمات)**
- صفحة قد تكون فارغة فعلًا أو صورة منخفضة الدقة جدًا.
- جرّب دقة أعلى: المسح بـ300 DPI على الأقل (نوصي بها).
- افتح "تقرير الجودة" (قائمة عرض) — الصفحات المشبوهة مدرجة سببًا.

**فشل صفحات معينة دائمًا**
1. "إعادة معالجة الصفحات الفاشلة" (تُعاد الفاشلات فقط).
2. لو استمرت: غالبًا الصفحة تالفة في الأصل — راجع تقرير الجودة لسبب كل فشل.
3. جرّب ملف "مسح ضعيف" لتلك الصفحات.

**التحويل بطيء**
- الملف "سريع" (250 DPI بلا معالجة) أسرع بثلاثة أضعاف تقريبًا.
- للإنجليزية فقط: اختر لغة "الإنجليزية" — النموذج fast أسرع. (العربية تستخدم best دائمًا للدقة.)
- زد "خيوط المعالجة" من الإعدادات (الافتراضي: أنوية − 1).

**استهلاك ذاكرة عالٍ أثناء كتاب ضخم**
لن يحدث نظريًا (معالجة صفحة-بصفحة). لو رصدته: قلّل الخيوط إلى 1-2 وأبلغنا. في اختبارنا الفعلي: 500 صفحة = ذروة ~366 MB.

## الماسح الضوئي

**"No WIA scanners found"**
- الماسح غير مثبت عند التشغيل أو تعريفه لا يدعم WIA — ثبّت تعريف الشركة المصنّعة.
- جرّب زر المسح من تطبيق ويندوز "الصحفف/Windows Fax and Scan" للتأكد أن الجهاز يعمل أصلًا.

**المسح الصامت لا يعمل لكن المعالج يعمل**
بعض الأجهزة لا تدعم النقل الصامت. استخدم "المسح بمعالج ويندوز" (يدعم المغذاة التلقائية ADF أيضًا).

**الصفحات الممسوحة باهتة/مائلة**
امسح بـ300 DPI على الأقل. ثم حوّل بملف "مسح ضعيف" (تصحيح ميل + تباين + عتبة تكيفية + إزالة حواف).

## قارئ الشاشة

**NVDA لا يعلن عن التقدم**
1. قائمة NVDA ← تفضيلات ← إعدادات ← "تقديم الكائن" ← فعّل "الإعلان عن التغييرات الديناميكية".
2. الحالة قابلة للقراءة دائمًا يدويًا: `NVDA+B` يقرأ النافذة كاملة (الصفحة الحالية/النسبة/المتبقي...).
3. قسم "السجل" يعرض كل الإعلانات نصًا — تنقّل إليه بـ F6 ثم اقرأ بـ `NVDB+سهم-أسفل`… (أي مفاتيح القراءة الاعتيادية).

**JAWS صامت**
فعّل الإعلان عن المحتوى الديناميكي من Options ← Basics، أو اقرأ النافذة بـ `Insert+B`.

**الإعلانات كثيرة/مزعجة**
الإعدادات ← أطفئ "الإعلانات المفصلة (كل صفحة)" — تعود للموجز (كل 10 صفحات) + الأحداث المهمة فقط.

**خط الواجهة صغير**
الإعدادات ← "حجم خط الواجهة" ← أعد تشغيل التطبيق.

## المخرجات

**Word يفتح الملف لكن النص العربي يظهر متجاورًا غريبًا**
تأكد أن الوضع "متاح للوصول" (الافتراضي). وضع "محافظ على التخطيط" يعطي فقرات سطر-بسطر.

**البحث في PDF القابل للبحث لا يجد كلمة عربية**
- جرّب كلمة واحدة قصيرة (البحث يعمل على مستوى الكلمة).
- بعض العارضات القديمة تتعامل مع RTL بشكل رديء — جرّب Adobe Reader أو SumatraPDF.

**HTML يفتح بترميز خاطئ في متصفح قديم**
الملف UTF-8 مع `<meta charset="utf-8">` — استخدم متصفحًا حديثًا (Edge/Chrome/Firefox).

## السجلات

- سجل التطبيق: `%APPDATA%\Iqra\logs\iqra.log` (دوّار).
- سجل المشروع: داخل مجلد `<الملف>.iqra_project\log.txt` — افتحه من قائمة "عرض".
- الحالة والنتائج الوسيطة: نفس المجلد (`pages\page_000001.json` ...).

## إعادة التثبيت/الإزالة

المزيل من "إعدادات ← تطبيقات" أو `unins000.exe`. إعداداتك في `%APPDATA%\Iqra` تبقى بعد الإزالة (احذفها يدويًا إن أردت).
