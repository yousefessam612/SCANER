# البنية الهندسية / Architecture

```
├── app/
│   ├── __main__.py          # نقطة الدخول: python -m app [convert|resume|batch|selftest]
│   ├── cli.py               # واجهة سطر الأوامر (عربية أولًا)
│   ├── paths.py             # تحديد tessdata (مضمَّن runtime/ أو نظامي)
│   ├── config.py            # الإعدادات (~/.config/Iqra أو %APPDATA%\Iqra)
│   ├── strings.py           # جدول النصوص ثنائي اللغة
│   ├── selftest.py          # فحص ذاتي: OCR حقيقي + كل الكُتّاب + بحث عربي
│   ├── exporting.py         # منسّق التصدير (كل الكُتّاب + تقرير الجودة)
│   ├── core/
│   │   ├── languages.py     # تحليل النص: اتجاه، سكربت، إصلاح العربي المعكوس، كاشفات الطبقات التالفة
│   │   ├── textclean.py     # تنظيف حتمي + بناء الكتل (عناوين/فقرات/قوائم) بإشارات بنية
│   │   ├── preprocess.py    # رمادي/Otsu/ضباب/عتبة — إنقاذ الصفحات المعادية للـOCR
│   │   ├── ocr.py           # tesserocr: كلمات+أسطر، سلّم محاولات مُقيَّم + دمج الأسطر المفقودة
│   │   ├── pagequality.py   # قياس الصفحة + تقييم OCR (كثافة الحبر، أسطر مفقودة بموج الصفوف)
│   │   ├── pdfutil.py       # تصنيف الصفحات (نص/صورة)، الحكم على الطبقة بعد محاولة إصلاحها
│   │   ├── rawpdf.py        # بانِي PDF قابل للبحث: طبقة عربية مخفية Identity-H + ToUnicode كامل
│   │   ├── engine.py        # صفحة-بصفحة: قياس/تقييم/سلّم/دمج، استئناف ذرّي، عزل أعطال
│   │   └── jobstate.py      # حالة المشروع: حفظ ذرّي بعد كل صفحة
│   └── writers/
│       ├── docx_writer.py   # DOCX حقيقي النص: <w:bidi/> + <w:rtl/> + خط CS عربي
│       ├── txt_writer.py    # TXT نظيف (UTF-8 BOM)
│       ├── html_writer.py   # HTML دلالي dir/lang لكل عنصر
│       └── report_writer.py # تقرير الجودة الحتمي (JSON + TXT)
├── tests/                   # 50 اختبارًا حقيقيًا (OCR فعلي على صفحات مولّدة)
├── runtime/tessdata/        # ara (best) + eng (fast) traineddata — تُوزَّع مع التطبيق
├── tests/fixtures/          # خط Amiri (OFL) لتوليد صفحات الاختبار
├── iqra.spec / entry.py     # تجميع PyInstaller
└── installer.iss            # سكربت Inno Setup (ويندوز)
```

## خط المعالجة

```
PDF ──▶ تصنيف الصفحة ──┬─ نص حقيقي (موثوق) ──▶ استخراج منسق
                        └─ صورة/طبقة غير موثوقة ──▶ رسم DPI حسب الملف
                                   ──▶ قياس الصفحة (عرض/ارتفاع/سطوع/تباين/حبر/ضوضاء/ميل)
                                   ──▶ سلّم محاولات OCR (مُقيَّم ومُختار بأدلة)
                                   ──▶ التحقق من الجودة (نجاح/مشكوك/فاشل)
                                   ──▶ إعادة محاولة تلقائية عند الشك
                                   ──▶ إعادة ترتيب قراءة + تنظيف + بناء كتل
        ──▶ نتيجة صفحة JSON على القرص ──▶ حالة ذرّية ──▶ التالية…
وبعد الاكتمال ──▶ كُتّاب البث (DOCX/TXT/HTML/PDF) + تقرير الجودة + تقرير التدقيق
```

## المعالجة الصفحية الجنائية (Page-by-Page Forensics)

كل صفحة تُعالَج **مستقلة**: تُقاس، تُقيَّم نتيجتها، وتُعاد معالجتها بتهيئة أخرى عند الشك، ويُحفظ كل ذلك للتدقيق.

### 1) قياسات الصفحة قبل OCR — `pagequality.measure_page`
العرض، الارتفاع، النسبة، DPI، السطوع، التباين، نسبة الحبر، الضوضاء (انحراف عن وسيط)، و**الميل بالدرجات**.
تحذير هندسي مقيس: قياس الميل يُتخطّى على الصفحات غير الشبيهة بالمستند (داكنة جدًا أو تباينها < 20) لأن منحنى الإسقاط يكون مسطّحًا فيتشبّع المُقدِّر عند حدّ البحث (قِيس فعليًا: صفحة داكنة أعطت ‑8° خطأً).

### 2) التحقق من الجودة — `pagequality.assess_ocr`
لا تُقبل صفحة لأن Tesseract أرجع 0. الفحوص حتمية:
صفحة فارغة بلا حبر = **نجاح** (لا محاولات عبثية) · نص فارغ مع وجود حبر = مشكوك · أسطر بيانات آلية ≥ نصف الأسطر = مشكوك · طبقة نص غير موثوقة (`languages.text_layer_reason`) = مشكوك · ثقة < 35% = مشكوك · > 50% كلمات ضعيفة الثقة = مشكوك · تكرار سطر ≥ 5 مرات = مشكوك · رموز بلا كلمات = مشكوك · **ميل ≥ 0.9°** = مشكوك · **صفحة منخفضة التباين** = مشكوك · كثافة كلمات أقل بكثير من المتوقع = مشكوك.
وكل نتيجة تحصل على **درجة** (ثقة + اكتمال − عقوبات) للمقارنة بين المحاولات.

### 3) سلّم إعادة المحاولة الحتمي — `ocrladder`
يرتب المحاولات من الأرخص/الأرجح، ويتوقف فورًا عند نتيجة جيدة:
`chosen` → `psm4_single_column` → `rerender_300dpi` (إن كان الرسم صغيرًا) → `poor_scan_pipeline` (ضوضاء/ميل) → `contrast_otsu` → `rerender_400dpi` → `psm6_block`. الحد الأقصى قابل للضبط (`ocr_max_attempts`, افتراضي 4) — لا عشرات التركيبات.

### 4) الإنقاذ بالاتجاه — `_orientation_rescue`
إن فشلت كل المحاولات، يُشغَّل OSD ويُختبر تدوير الصفحة، ويُقبل فقط إن حسّن النتيجة.

### 5) الأدلة والتقارير
- `debug/page_XXXX/`: `original.png`, `processed.png`, `raw.txt`, `raw.tsv`, `diagnostic.json` (القياسات + كل المحاولات بأسبابها), `final.txt` — عند تفعيل `keep_debug_artifacts`.
- `ocr_audit_report.json/.txt`: لكل صفحة الحالة/المصدر/الثقة/الكلمات/الزمن/عدد المحاولات/السبب، وملخّص (ناجحة/مشكوكة/فاشلة/تحتاج انتباهًا + الصفحات المفقودة إن وُجدت). تُكتب دائمًا عند التصدير ولا تظهر أبدًا في مستند المستخدم.
- أدوات التشخيص: `tools/forensic_run.py` (تشغيل تشخيصي كامل)، `tools/direct_ocr_compare.py` (مقارنة مباشرة مع Tesseract CLI على نفس الصورة لعزل مكان الخلل)، `tools/validate_outputs.py` (نقاء المخرجات)، `tools/measure_arabic_accuracy.py` (قياس WER).

### 6) عزل مكان الخلل
قاعدة التشخيص: إن أنتج Tesseract المباشر نصًا صحيحًا بينما أخرج التطبيق نصًا خاطئًا فالخلل في طبقتنا لا في OCR. مقياس آلي: تشابه كلمات مخرج Tesseract المباشر مقابل مخرج التطبيق (قِيس 0.992 على صفحة اختبار) و«مخرج التطبيق مقابل المستند النهائي» (0.995) — أي أن التحليل والترتيب والتنظيف أمينة، ولا تُحذف معلومات.


## القرارات الهندسية الجوهرية

### 1. إصلاح العربية المعكوسة — طبقةً وOCRًا (المشكلة العربية الكبرى)
- **طبقات PDF النصية**: المستخرج الحديث يُعيد محارف القراءة تلقائيًا؛ المتبقي أشكال العرض (NFKC يفكّها) واحتمال تخزين معكوس. لكل سطر: نطبع القراءة الطبيعية (أقصى اليمين أولًا بعد NFKC)؛ فإن بدا معكوسًا إحصائيًا (`looks_reversed_arabic`: نسب «ال» الابتدائية مقابل «لا» النهائية) طبقنا إعادة البناء الكاملة (عكس الكلمات + عكس حروف كل كلمة مع حماية الجُزر اللاتينية/الرقمية).
- **كلمات OCR**: تخرج معكوسة الحروف بترتيب بصري حتميًا → `fix_word_to_logical` لكل كلمة (عكس + NFKC + تنظيف ليغاتورات) وإعادة بناء السطر من إحداثيات x.
- حتمي 100%، بلا AI. أمثلة مثبتة: `ةغللا` → `اللغة`، `ﻡﻱﺡﺭﻝﺍ ﻥﻡﺡﺭﻝﺍ` → `الرحمن الرحيم`.

### 2. طبقة النص في PDF القابل للبحث (بناؤها بأنفسنا)
مخرج PDF من Tesseract يرسم الكلمات العربية بمصفوفة معكوسة، فتستخرجه أغلب العارضات معكوسًا (`ةغللا` بدل `اللغة`) — البحث العربي يفشل. لذلك نبني الطبقة بأنفسنا (core/rawpdf.py): لكل كلمة، محارف بترتيب منطقي مع CIDs = Unicode وToUnicode CMap، حجم الخط = تقدّم المحرف (فيلتحم النص عند الاستخراج)، عربي من اليمين ولاتيني من اليسار، `3 Tr` مخفي. النتيجة: بحث ونسخ عربي سليم مع بقاء الصفحة الأصلية بايت-بايت.

### 3. إصلاح PDF العربي "سيئ الصنع"
طبقات نص عربية كثيرة في الواقع تخزن أشكال العرض معكوسة. `fix_visual_arabic`: NFKC يفك الأشكال إلى حروف أساس، ثم عكس المقاطع العربية (سطر عربي سائد = عكس كامل؛ سطر مختلط = عكس المقاطع العربية فقط). مثال مُثبت: `ﻡﻱﺡﺭﻝﺍ ﻥﻡﺡﺭﻝﺍ` → `الرحمن الرحيم`.

### 3ب. كشف الطبقات النصية التالفة (سبب شكوى «كل البرامج تُخرج نفس الخطأ»)
كثير من ملفات PDF العربية في الواقع تحمل طبقة نص فاسدة (OCR رديء من أداة أخرى، أو خريطة ToUnicode معطوبة): الصفحة المرئية سليمة والنص المستخرج فاسد. البرامج التي تثق بالطبقة تُخرج الفساد نفسه. لذلك يفحص `pdfutil.page_check()` الطبقة قبل الثقة بها ويُعيد `(kind, reason)`:

| الكاشف | القياس الفعلي | العتبة |
|---|---|---|
| `broken_marks` | شذوذ أزواج العلامات (علامة بعد علامة، عدا شدة+حركة وسكون+ألف خنجرية) | > 0.25 لكل حرف |
| `mark_flood` | علامات ÷ حروف | > 1.35 (المشكول السليم ≤ 1.02) |
| `foreign_letters` | حروف 063B–063F والفارسية (تظهر عند تعطُّل خريطة الترميز) | > 3% |
| `reversed_order` | نسبة الكلمات المبتدئة بـ«ال» < 5% مع المنتهية بـ«لا» > 20% (ينعكس تمامًا في الطبقات المعكوسة) | كما هو |

عند التحقق يُهمَل النص ويُرسم للصفحة ثم يُشغَّل OCR (النموذج best للعربية) — فالعربية السليمة تأتي من الصورة. المعايرة جرت على عينات حقيقية: نص عادي (0 شذوذ)، مشكول (0.84–0.85 علامة/حرف، 0.09–0.16 شذوذ)، قرآني بعلامات وقف (1.02)، مقابل طبقات تالفة (1.58–2.00 علامة/حرف، 0.66–1.14 شذوذ) — الهامش آمن. لا يُدرج الكاشف نصًا مشكولًا أو قوائم أسماء (مُختبر) ضمن التالف. يمكن للمستخدم تعطيل الكشف بـ`trust_text_layer`.

### 3ج. كشف الطبقات «الآلية» (مُخرَج OCR خام مدفون في الملف)
حالة واقعية أخرى أبلغ عنها مستخدم: طبقة النص داخل PDF هي **مُخرَج TSV آلي** (أرقام هندسة + قيم ثقة مثل `91.518349` + الكلمات). القياس على الملف الحقيقي: 92% كلمات رقمية، 8% نصية، 142 tab، 12 رقم ثقة — مقابل صفحات حقيقية: 0.83–1.00 نصية، 0 tab، 0 أرقام ثقة. الكاشف `text_layer_machine_reason` يستخدم شروطًا **تجميعية** (كل قاعدة تتطلب إشارتين أو أكثر) فلا يُعلّم جداول الأرقام المشروعة ولا النصوص العلمية العشرية (مُختبر):

| القاعدة | الشرط |
|---|---|
| `layer:tab_delimited` | 5+ أحرف جدولة مع نسبة نصية < 0.6 |
| `layer:machine_numbers` | 4+ أرقام بأربع منازل عشرية أو أكثر مع نسبة نصية < 0.6 |
| `layer:not_prose` | 75%+ كلمات رقمية مع نسبة نصية < 0.3 (و30 كلمة فأكثر) |

النتيجة: الصفحة تُرسم ويُشغَّل عليها OCR، ونصها السليم يأتي من الصورة.

### 3ج. ترتيب القراءة وطبقة PDF القابل للبحث
- ترتيب القراءة يُبنى هندسيًا (عكس قائمة السطر العربي ثم عكس المقاطع اللاتينية داخلها).
- طبقة PDF القابل للبحث تُبنى يدويًا (`rawpdf`): خط Type0 بترميز Identity-H، **ToUnicode كامل لكل المحارف المستخدمة** (بدونه يفشل البحث)، والكلمة تُكتب بالترتيب البصري (العقد: النص الداخل منطقي → يُقلب بصريًا مع حماية الجُزر اللاتينية) لأن المستخرجات تطبق BiDi فتُعيد المنطقي، وTz يضبط تقدّم الكلمة على صندوقها، وفراغ صريح في منتصف الفجوة بين كل كلمتين. أُثبت اختبارًا: استخراج منطقي + `search_for` يعمل.
- حالة الطبقة الأصلية تحكم بناء الصفحة: `trusted` (سليمة → نسخ بايت-بايت بلا طبقة إضافية)، `none` (نسخ + طبقتنا)، `broken` (رسم الصفحة صورةً + طبقتنا — لا تُنسخ الطبقة الفاسدة).
- **الدمج في /Contents يكون بإلحاق ستريم جديد داخل مصفوفة** — تعديل ستريمات الصفحات متعددة الستريعات يفسدها (مُختبر على صفحة بـ32 ستريمًا).

### 4. الذاكبة المحدودة
منتِج واحد يقرأ PDF (كائن PyMuPDF لكل خيط) ويرسم صفحة → PNG bytes → طابور `maxsize = workers×2`. مستهلكون (عددهم قابل للضبط) يفككون الضغط ويعالجون ويثبتون النتيجة على القرص ثم يحررون كل شيء. كل صورة معيشة ≤ صفحتان لكل خيط. مثبت فعليًا: 500 صفحة → ذروة RSS ~366 MB.

### 5. الاستئناف الحتمي
الحالة JSON تُكتب بـ tmp + `os.replace` (ذرّي) بعد **كل** صفحة. الفشل بين الحفظين يخسر الصفحة الجارية فقط. عند الفتح: "استئناف من الصفحة التالية؟" — ولا يُعاد لمس المكتمل أبدًا (اختبار يتحقق من mtime ملف النتيجة).

### 6. عزل الأعطال
Tesseract يعمل كعملية فرعية — انهياره لا يُسقط التطبيق. صفحة فاشلة: محاولة إعادة واحدة كاملة ثم تسجيل والمتابعة. صفحة تالفة حقيقيًا (تيار JPEG مُخرب) مُختبرة: لا توقف ولا تجميد.

### 7. اختيار نموذج اللغة
قِسنا فعليًا: النموذج fast يُسقط سطرًا عربيًا-إنجليزيًا مختلطًا بالكامل (32 كلمة مقابل 40 لـbest). القرار: العربية ⇒ best دائمًا (حتى في الملف السريع)، الإنجليزية ⇒ fast.

### 8. إعلانات قارئ الشاشة
Qt/Windows يعرض UIA. نطلق `QAccessible.Event.Alert` على تسمية الحالة المخصصة + نكتب النص في السجل المرئي وفي العنوان — ثلاث مسارات، لا يضيع الإعلان.

### 9. لماذا PowerShell للـWIA؟
WIA COM عبر `powershell -Command` بلا أي اعتماد إضافي: تعداد الأجهزة، مسح صامت، معالج ويندوز (ADF). يعمل دون إنترنت ودون تثبيت شيء.

## حدود معروفة (موثقة بصدق)
- OCR ليس 100% أبدًا — تقرير الجودة يسرد المشبوهات؛ لا نزعم الكمال.
- استخراج النص من DOCX/TXT/HTML ممتاز؛ لكن النسخ من PDF القابل للبحث يعتمد على العارض (بحث الكلمات يعمل في كل العارضات القياسية — اختبرناه بـMuPDF).
- كشف الجداول أساسي حتمي؛ حين لا يكون موثوقًا نُبقي النص بترتيب القراءة بدل جدول مكسور.
- دقة ملف "سريع" أقل مع الخطوط الصغيرة جدًا — إن كانت الدقة أهم من السرعة استخدم متوازن/دقة عالية.
