# دليل إمكانية الوصول / Accessibility Guide

تم بناء اقرأ ليكون قابلًا للاستخدام بقارئ الشاشة من الدرجة الأولى — لا مجرد واجهة تعمل.

## التشغيل مع NVDA (موصى به)

### الإعداد
1. شغّل NVDA قبل تشغيل اقرأ.
2. تأكد أن خيار "الإعلان عن التغييرات الديناميكية للمحتوى" مفعل (افتراضيًا مفعّل): قائمة NVDA ← تفضيلات ← إعدادات ← تقديم الكائن.
3. شغّل اقرأ من أيقونة سطح المكتب أو قائمة ابدأ.

### كيف يصل NVDA إلى إعلانات التقدم؟
يستخدم اقرأ أحداث **UI Automation Alert** عبر منطقة إعلان مخصصة (السطر الغامق أسفل قسم السجل). اسمع:
"بدأ التحويل." / "جارٍ معالجة الصفحة 40 من 850." / "تم الإيقاف المؤقت." / "اكتمل التحويل مع 3 صفحات فاشلة." / "تم التصدير بنجاح."

الإعلانات الموجزة (كل 10 صفحات) هي الافتراضية كي لا يُغرق القارئ. فعّل "الإعلانات المفصلة" من الإعدادات لتسمع كل صفحة.

### قراءة الحالة في أي لحظة
- `NVDA+B` (قراءة النافذة) يقرأ كل التسميات: الصفحة الحالية، النسبة، المتبقي...
- `NVDA+T` يقرأ عنوان النافذة.
- قسم "التقدم" فيه ثمانية صفوف "تسمية: قيمة" — تنقّل إليها بـ Tab أو F6 ثم الأسهم.
- قسم السجل: `Ctrl+End` لآخر حدث. يمكنك فتح ملف السجل نفسه من قائمة عرض.

### التنقل الأساسي
- `F6` يدور بين الأقسام الخمسة، ويُعلن اسم القسم.
- `Tab`/`Shift+Tab` داخل القسم، `Space` للأزرار ومربعات الاختيار، الأسهم للقوائم المنسدلة.
- كل زر وقائمة له اسم واضح بالعربية، والأزرار المعطلة يتخطاها القارئ تلقائيًا.
- رسائل الحوار (استئناف؟ / اكتمل مع فاشلات) هي نوافذ حوار قياسية تُقرأ تلقائيًا — زر افتراضي واضح يمكن تفعيله بـ Enter.

## التشغيل مع JAWS
- يعمل عبر UIA مثل NVDA. إن لم تُسمَع الإعلانات: فعّل الإعلان عن التغييرات الديناميكية من إعدادات JAWS (Options ← Basics ← "Announce dynamic content changes").
- استخدم `Insert+B` (Read Window) لقراءة الحالة، و`Insert+T` للعنوان.
- وضع النماذج الافتراضي في JAWS يعمل مع مربعات الاختيار والقوائم المنسدلة.

## التشغيل مع قارئ Windows (Narrator)
- `CapsLock+D` يقرأ محتوى النافذة.
- الإعلانات الديناميكية تُلتقط تلقائيًا (وضع "أخبرني" الافتراضي).
- مفتاح Narrator `CapsLock+Space` ثم الأسهم للتنقل بالعناوين داخل الحوار.

## تصميم الواجهة — القواعد الملتزم بها
1. **كل عنصر تفاعلي له اسم وصول**: تم اختبار ذلك آليًا — اختبار يمسح كل زر وقائمة ومربع اختيار ويتحقق من وجود الاسم.
2. **لا اعتماد على اللون وحده ولا الرموز وحدها**: الحالة نصية ("فاشلة: 3") — لا تمييز باللون فقط.
3. **كل شيء بلوحة المفاتيح**: لا يوجد إجراء يتطلب الفأرة (راجع جدول الاختصارات في دليل الاستخدام).
4. **قوائم حقيقية** بأسماء وصول (Alt+حرف) — لا أشرطة أدوات غامضة.
5. **الواجهة نفسها RTL** عند اختيار العربية، مع خط قابل للتكبير من الإعدادات.
6. **لا نوافذ عائمة غير مُعلنة** ولا تحديثات صامتة للحالة — كل تغيير مهم يمر عبر الإعلانات أو الحوار.

## الواجهة لا تتجمد أبدًا
OCR يجري في خيوط خلفية؛ الواجهة تستجيب دائمًا. يمكنك التنقل وقراءة السجل أثناء معالجة 1000 صفحة.

## أجهزة braille
مخرجات DOCX وTXT تحافظ على فقرات نظيفة وعناوين حقيقية — تظهر على سطر braille ببنية صحيحة، والنص العربي منطقي الترتيب (RTL) لا معكوس.

## إن لم تسمع شيئًا
راجع [TROUBLESHOOTING.md](TROUBLESHOOTING.md) قسم "قارئ الشاشة لا يعلن".
