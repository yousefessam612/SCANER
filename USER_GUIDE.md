# دليل الاستخدام / User Guide

اقرأ — محوّل المستندات إلى صيغ مقروءة بقارئ الشاشة. يعمل دون اتصال بالإنترنت تمامًا.

## البدء السريع (5 خطوات)

1. **افتح PDF**: زر "فتح PDF" أو `Ctrl+O`. ستسمع: "تم تحميل اسم_الملف — عدد الصفحات صفحة."
2. **اختر لغة التعرف**: `Ctrl+L` أو القائمة — العربية، الإنجليزية، أو العربية + الإنجليزية (الافتراضي).
3. **اختر الجودة**: متوازن (موصى به) / سريع / دقة عالية / مسح ضعيف.
4. **اختر الصيغ ومجلد الإخراج**: DOCX وTXT وHTML وPDF قابل للبحث — كلها مفعّلة افتراضيًا.
5. **اضغط `Ctrl+Enter`** لبدء التحويل. اسمع التقدم: "جارٍ معالجة الصفحة 10 من 850."

عند الاكتمال تُصدَّر الملفات تلقائيًا وتسمع "تم التصدير بنجاح."

## الشاشة الرئيسية — الأقسام (تنقّل بينها بـ F6)

| القسم | المحتوى |
|---|---|
| المستند المصدر | فتح PDF / عدة ملفات / اسم الملف |
| خيارات التحويل | اللغة، الجودة، الصيغ، مجلد الإخراج |
| التنفيذ | ابدأ / إيقاف مؤقت / استئناف / إلغاء / إعادة الفاشلات / تصدير |
| التقدم | الصفحة الحالية، الإجمالي، المكتمل، الفاشل، النسبة، المنقضي، المتبقي، المرحلة |
| السجل | كل الأحداث + منطقة إعلانات قارئ الشاشة |

## اختصارات لوحة المفاتيح

| الاختصار | الوظيفة |
|---|---|
| `Ctrl+O` | فتح ملف PDF |
| `Ctrl+Shift+O` | فتح مشروع محفوظ |
| `Ctrl+Enter` | ابدأ التحويل |
| `Ctrl+P` | إيقاف مؤقت |
| `Ctrl+R` | استئناف |
| `Esc` | إلغاء |
| `Ctrl+S` | حفظ المشروع |
| `Ctrl+E` | تصدير |
| `Ctrl+L` | لغة التعرف الضوئي |
| `F6` | التنقل بين الأقسام |
| `Alt` + الحرف المميز | فتح القوائم (ملف، الماسح، عرض، إعدادات، مساعدة) |
| `Tab` / `Shift+Tab` | التنقل بين عناصر القسم |

## المشاريع والاستئناف

كل تحويل يُنشئ مشروعًا تلقائيًا بجانب ملف PDF (مجلد `.iqra_project`). يحفظ الحالة بعد **كل صفحة**.

- أغلق البرنامج أثناء 1000 صفحة؟ عند فتح المشروع: "هذا المشروع مكتمل حتى الصفحة 438. استئناف من 439؟"
- حفظ المشروع: `Ctrl+S`. فتحه: `Ctrl+Shift+O` أو قائمة ملف ← المشاريع الأخيرة.
- "إعادة معالجة الصفحات الفاشلة" تعيد الفاشلات فقط، بلا إعادة معالجة المكتمل.

## الكتب الضخمة

- **لا يوجد أي حد لعدد الصفحات**: البرنامج يعالج الكتاب كاملًا (10، 20، 100، 500، 1000 صفحة أو أكثر) حسب قدرات جهازك. الاختصار الصريح الوحيد هو ما تختاره أنت في «نطاق الصفحات».
- يظهر لك قبل البدء: **«الصفحات المطلوب معالجتها: 850»** — وهذا الرقم هو ما سيُعالج فعلًا.
- نطاق الصفحات (اختياري): «من صفحة» و«إلى صفحة». اتركهما صفرًا لمعالجة **الكتاب كامل** (الافتراضي). مثال: من 20 إلى 50 يعالج 31 صفحة فقط.
- معالجة صفحة-بصفحة عبر طابور محدود: لا يُحمَّل الكتاب في الذاكرة (اختبار حقيقي 500 صفحة: ذروة الذاكرة ~366 MB، والنتائج الوسيطة تُحفظ لحظيًا).
- «تقسيم المخرجات كل N صفحة» (الإعدادات): ينتج `Book_001-100.docx` و`Book_101-200.docx`...
- علامات `[Page 15]` (مفعّلة افتراضيًا) تساعدك على الإشارة لصفحة الكتاب الأصلي.
- إن توقف الجهاز في المنتصف: عند إعادة الفتح «استئناف من الصفحة 439؟» — ولا يُعاد معالجة ما اكتمل.

## التحقق من نقاء المخرجات (أداة مساعدة)

للتأكد بنفسك أن المخرجات نصٌّ فقط بلا أي بيانات آلية (أرقام إحداثيات أو قيم ثقة):

```
python tools\validate_outputs.py "مجلد الإخراج"
```

يطبع `CLEAN` لكل ملف سليم، ويُدرج أي سطر يحمل بيانات آلية مع رقم السطر. النتيجة المطلوبة: `no OCR metadata found`.

## الماسح الضوئي (قائمة "الماسح الضوئي")

1. **اختيار الماسح**: قائمة أجهزة WIA المتصلة.
2. **مسح صفحة (بدون نافذة)**: صامت — مناسب لدفعة سريعة.
3. **المسح بمعالج ويندوز**: نافذة ويندوز الأصلية — تدعم المغذاة التلقائية ADF.
4. **إنشاء PDF من الصفحات الممسوحة** ثم يُفتح كمشروع للتحويل.

الدقة: 200 (أسرع) / **300 (الموصى به)** / 400 / 600 (أبطأ وأدق للخط الدقيق).

## عدة ملفات (دفعة)

"عدة ملفات" ← اختر Book1.pdf وBook2.pdf وBook3.pdf — تُعالج **تباعًا** (ليست كلها في الذاكرة)، ولكل واحد مشروع ومخرجاته.

## PDF المختلط (نص + صور)

كل صفحة تُفحص مستقلة: تحتوي نصًا حقيقيًا؟ يُستخرج مباشرة (بلا OCR غير ضروري). صورة فقط؟ يُمرَّر على OCR. تعمل تلقائيًا.

## طبقة نص عربية تالفة (مهم!)

بعض ملفات PDF العربية — خاصة ما أنتجته برامج مسح أخرى — تحتوي طبقة نص فاسدة: المستند يُعرض سليمًا لكن النص المستخرج منه حروف وحركات متبعثرة أو معكوسة. كل البرامج التي تثق بهذه الطبقة تُخرج الفساد نفسه.

اكتشف اقرأ هذه الحالات تلقائيًا (بلا AI، بمعايرة إحصائية على عينات حقيقية) **ويشغّل OCR على الصورة المرئية بدلًا من الطبقة التالفة** — فتحصل على نص عربي صحيح. الصفحات المعالجة بهذه الطريقة مُدرجة في تقرير الجودة.

النص المشكول سليمًا (كتب الأدعية والأحاديث) لا يُعلَّم خطأً — مُختبر على نصوص مشكولة كثيفة. وللتحكم: الإعدادات ← «الثقة بالطبقات النصية التالفة» (لا ننصح بتفعيلها).

## جودة المعالجة

| الملف | المعالجة |
|---|---|
| سريع | 250 DPI، رمادي فقط — للكتب النظيفة الضخمة |
| متوازن | 300 DPI، تباين تلقائي، عتبة Otsu، تصحيح ميل |
| دقة عالية | 400 DPI، تنعيم + عتبة تكيفية + إزالة حواف + نموذج best |
| مسح ضعيف | 400 DPI، معالجة قوية للمسحات الرديئة |

ملاحظة: للغة العربية يُستخدم نموذج **best** تلقائيًا حتى في الوضع السريع (النموذج fast يُسقط أسطرًا عربية/إنجليزية مختلطة — أثبتنا ذلك بالقياس).

## تقرير الجودة (قائمة عرض)

لا يمكن ضمان دقة OCR 100% أبدًا. التقرير يسرد:
- صفحات فشلت معالجتها
- صفحات ثقة منخفضة (مسح ردئ)
- صفحات نسبة كلماتها منخفضة الثقة عالية

راجع هذه الصفحات يدويًا في الأصل. زر "إعادة معالجة الصفحات الفاشلة" لإعادة المحاولة.

## الإعدادات (قائمة إعدادات)

- لغة الواجهة (عربية/إنجليزية) وحجم الخط
- عدد خيوط المعالجة (تلقائي = أنوية المعالج − 1، بحد أقصى 4)
- **وضع تقسيم الصفحة PSM**: تلقائي (افتراضي) / «عمود واحد — أفضل للكتب العربية» (أدق قليلًا للكتب العربية ذات العمود الواحد) / كتلة منتظمة / تلقائي مع كشف الاتجاه
- وضع DOCX: **متاح للوصول** (فقرات وعناوين نظيفة — موصى به) أو **محافظ على التخطيط** (سطر بسطر بأحجام مشابهة)
- سياسة الأرقام: كما هي / تحويل إلى 123 / تحويل إلى ١٢٣
- إصلاح النص العربي المعكوس (لملفات PDF سيئة الصنع)
- **الثقة بالطبقات النصية التالفة**: يعطّلها الكشف التلقائي (لا ننصح به)
- دمج الأسطر في فقرات
- فرض OCR حتى مع وجود نص
- مسار Tesseract مخصص + زر "اختبار" للتشخيص الذاتي
- الإعلانات المفصلة (كل صفحة) أو الموجزة (كل 10 صفحات)

## سطر الأوامر (للمتقدمين)

```
IqraCLI.exe convert book.pdf -l ara+eng -p balanced -f docx,txt,html,pdf -o out_dir
IqraCLI.exe convert a.pdf b.pdf c.pdf -o out_dir        # دفعة
IqraCLI.exe resume project.iqra
IqraCLI.exe export project.iqra -o out_dir
IqraCLI.exe scan list
IqraCLI.exe selftest
```

## أين ملفاتي؟

في مجلد الإخراج: `اسم_الملف.docx` و`.txt` و`.html` و`اسم_الملف_searchable.pdf` و`اسم_الملف_quality_report.txt`. عند التقسيم: `اسم_الملف_001-100.docx` وهكذا.
