# دليل الاستخدام / User Guide

اقرأ — محوّل PDF إلى DOCX/TXT/HTML/PDF قابل للبحث، بواجهة عربية RTL متوافقة مع قارئ الشاشة، ويعمل دون اتصال بالإنترنت تمامًا.

## التشغيل

```bash
python -m app            # الواجهة الرسومية (الافتراضي)
run.bat                  # ويندوز: ينقر منه مباشرة — يجهز كل شيء أول مرة
```

سطر الأوامر متاح كاملًا أيضًا: `python -m app convert كتاب.pdf -f docx,pdf -o المخرجات`

## الواجهة الرسومية — الأقسام (تنقّل بـ Tab بين العناصر)

| القسم | المحتوى |
|---|---|
| المستند المصدر | حقل مسار PDF + زر «فتح PDF…» |
| خيارات التحويل | اللغة، الجودة، صيغ المخرجات (وورد/نص/HTML/PDF قابل للبحث)، مجلد المخرجات، «الإعدادات…» |
| التنفيذ | ابدأ التحويل / إيقاف مؤقت–استئناف / إلغاء / فتح مجلد المخرجات + الماسح الضوئي (ويندوز) |
| التقدم | شريط تقدم + «صفحة X من Y» |
| الحالة والسجل | ملصق الحالة (يُعلن لقارئ الشاشة) + سجل تفصيلي بكل صفحة |

## اختصارات لوحة المفاتيح

| الاختصار | الوظيفة |
|---|---|
| `Ctrl+O` | فتح ملف PDF |
| `Ctrl+Enter` | بدء التحويل |
| `Ctrl+P` | إيقاف مؤقت / استئناف |
| `Ctrl+Alt+C` | إلغاء (يحفظ التقدم ويستأنف لاحقًا) |
| `Ctrl+,` | الإعدادات |

## قارئ الشاشة (NVDA / JAWS / الراوي)

- كل زر وحقل له اسم Accessible ووصف واضح.
- الحالات المهمة (البدء، كل 10 صفحات، الاكتمال، الإلغاء، الأخطاء) تُعلن عبر **UIA Alert** + ملصق الحالة + السجل — ثلاثة مسارات.
- الفتح بملف: `python -m app كتاب.pdf` يفتح الواجهة والملف محدد جاهزًا للتحويل.

## الاستئناف

عند فتح ملف له مشروع غير مكتمل يسألك البرنامج: **«استئناف من نفس النقطة» / «بدء من جديد» / «إلغاء»**. الإلغاء أو انقطاع الكهرباء يحفظ الصفحات المنجزة دائمًا.

## الماسح الضوئي (ويندوز فقط)

1. «مسح صفحة من الماسح» يفتح معالج ويندوز (يدعم المغذاة ADF) ويضيف الصفحة.
2. كرر لكل صفحة — العداد يظهر عدد الممسوح.
3. «أنشئ PDF من الممسوح» يبني PDF جاهزًا في حقل المصدر — ثم ابدأ التحويل كالمعتاد.

## سطر الأوامر الكامل

```
python -m app convert ملف.pdf [خيارات]
  -l ara|eng|ara+eng      لغة التعرف (افتراضي ara+eng)
  -q fast|balanced|high   السرعة مقابل الدقة (DPI 200/300/400)
  -f docx,txt,html,pdf    صيغ المخرجات
  -o مجلد                 مجلد المخرجات
  --page-breaks           فاصل صفحة في الوورد بين صفحات PDF
  --no-trust-text-layer   فرض OCR دائمًا

python -m app resume "كتاب.iqra_project"    # استئناف
python -m app batch مجلد/                   # كل ملفات PDF في مجلد
python -m app scan --pages 5                # مسح 5 صفحات → PDF (ويندوز)
python -m app selftest                      # فحص ذاتي شامل
```

## النواتج

في مجلد المخرجات: `اسم_الملف.iqra_project/output/` — DOCX (وورد عربي RTL)، TXT (UTF-8 BOM)، HTML، `اسم_الملف_searchable.pdf`، وفي جذر المشروع `quality_report.txt`.

## نصائح للعربية

- الافتراضي ara+eng هو الأفضل للوثائق المختلطة.
- الملفات العربية «سيئة الصنع» (النسخ يخرج `ةغللا`) تُصلح تلقائيًا — بلا أي إعداد.
- للمسح الورقي الرديء جرّب «دقة عالية».
